package com.practice;

class Config{
    // TODO - change instance using this to using an
    //  instance of daemon.getProgramState - pass it an instance
    //  of 'this' or have it use a field passed in constructor
    static String programState = "1";
    static boolean isDatabaseSyncing = false;
}
